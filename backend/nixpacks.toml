[variables]
PYTHON_VERSION = "3.13.14"
UVICORN_PORT = "$PORT"

[phases.setup]
nixPkgs = ["python3.13", "uv"]

[phases.install]
cmds = ["uv pip install -r requirements.txt"]

[phases.start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
